class XMLAdaptor:
    """
    This class will facilitate the conversion to and from the XML fragments for
    use with the parsers and encoders. As there are several different schemas
    for XMI, we want it to fairly straight-forward to switch between them
    without having to update the parsers or encoder code
    """
    pass
